spring:
    application:
        name: discovery-service

server:
    port: 8050
eureka:
    client:
        registerWithEureka: false
        fetchRegistry: false
        service-url:
            defaultZone: http://localhost:8050/eureka
